%li{:class => "dropdown #{get_nav_link_class('maintenance')}"}
  = link_to "#", :class => "dropdown-toggle", :style => 'padding: 19px 4px 14px;', :data => {"toggle" => "dropdown"} do
    %i.fa.fa-wrench.fa-lg
    Maintenance
    %b.caret
  %ul.dropdown-menu.multi-level{:role => "menu"}

    - # Load module specific tab content if they exist
    - SystemConfig.transam_module_names.each do |mod|
      - view_component = "#{mod}_maintenance_sub_nav"
      - if lookup_context.template_exists?(view_component, 'shared', true)
        = render :partial => "shared/#{view_component}"
