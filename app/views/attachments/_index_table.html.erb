<div class="row">
	<div class="col-md-12">
		<table id="datatable" class="table table-condensed table-striped table-hover table-bordered">
			<thead>
				<tr>
					<th class="center">Type</th>
					<th class="center">Name</th>
					<th class="center">Filename</th>
					<th class="center">File size</th>
					<th class="center">Uploaded By</th>
					<th class="center">Uploaded On</th>
					<th class="center">Actions</th>
				</tr>
			</thead>
			<tbody>
				<% @attachments.each do |a| %>
					<tr>
						<td class="left"><%= a.attachment_type.name %></td>
						<td class="left"><%= a.name %></td>
						<td class="left"><%= a.original_filename %></td>
						<td class="right"><%= number_to_human_size(a.file_size) %></td>						
						<td class="left"><%= a.creator.name %></td>
						<td class="right"><%= format_as_date_time(a.created_at) %></td>
						<td class="center">
							<%= link_to inventory_attachment_path(@asset, a), :class => 'btn btn-success btn-xs' do %>
								<i class="fa fa-picture-o"></i> View
							<% end %>
							<%= link_to download_inventory_attachment_path(@asset, a), :data => {:confirm => "Do you want to download this file?"}, :class => 'btn btn-primary btn-xs' do %>
								<i class="fa fa-download"></i> Download
							<% end %>
						</td>
					</tr>
				<% end %>
			</tbody>
		</table>
	</div>
</div>

<%= render 'shared/datatable_javascript' %>
