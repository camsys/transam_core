.row
  .col-md-12
    .panel.panel-default.header-part
      .panel-heading
        .row
          .col-md-3
            %h3.panel-title
              %i.fa.fa-list
              Notices
          .col-md-9
            = render 'index_actions'
      .panel-body
        - if @notices.empty?
          %p No matching notices found
        - else
          = render :partial => 'index_table', :locals => {:notices => @notices}

= render 'index_scripts'
