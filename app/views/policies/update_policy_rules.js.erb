$('#rules-container').html("<%= escape_javascript(render 'rules') %>");
$('#spinner').hide();
reset_view();
<% if @message.present? %>
  transam.show_flash_message("<%= @message %>", "<%= @alert_class %>");
<% end %>
