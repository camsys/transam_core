<%= render 'organization_subnav' %>

<%= render(:partial => 'shared/form_errors', :locals => {:obj => @organization}) unless @organization.errors.empty? %>	

<%= render "#{@organization.class.name.underscore}_form" %>

<%= render 'scripts' %>