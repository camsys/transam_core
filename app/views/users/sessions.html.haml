.col-md-12
  .page-header
    %h2 Online Users
  .row
    = render 'sessions_table'

= render :partial => 'shared/table_scripts', :locals => {:table_id => 'user-sessions-table', :path_pattern => user_path("xxx")}

:javascript
  //var url = '#{sessions_users_path(:format => :js)}';
  //var interval = 60000;
  //setInterval(function(){
  //  $.ajax({
  //    url: url,
  //    cache: false
  //  });
  //}, interval);
