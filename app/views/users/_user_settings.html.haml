= format_field("Send Emails", format_as_yes_no(@user.notify_via_email) )
-# if @user.has_role? :manager
  =# format_field("Default Organization Filter", @user.user_organization_filter.name)
= format_field("Number of table rows to display", @user.num_table_rows)
