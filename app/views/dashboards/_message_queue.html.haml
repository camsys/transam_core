.panel.panel-default{:style => 'height: 200px;overflow-y: auto;'}
  - if messages.empty?
    .panel-body
      %p= "No messages."
  - else
    %ul.list-group
      - messages.each do |msg|
        = render :partial => 'messages/message_detail', :locals => {:msg => msg, :filter => filter}
