.panel.panel-default{:style => 'height: 300px;overflow-y: auto;'}
	- if objs.empty?
		%p= "No assets in queue."
	- else
		= render :partial => "assets/asset_compact_datatable", :locals => {:assets => objs}
