= render :partial => 'shared/form_errors', :locals => {:obj => @asset} unless @asset.errors.empty?

= render "#{@asset.class.name.underscore}_form"

= render 'form_scripts'

:javascript
  
  $(document).ready(function() {            
    transam.make_same_height('.form_part');    
  });
